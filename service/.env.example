# Copy to .env and adjust values, then `source .env` before running the service.

# ADC credentials for local dev (point to your service account JSON key)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Core service config
DOCFLOW_GCP_PROJECT=your-gcp-project-id
DOCFLOW_LOCATION=us-central1
DOCFLOW_DEFAULT_MODEL=gemini-2.5-flash
DOCFLOW_DEFAULT_TEMPERATURE=0.0

# Profile catalog (choose ONE backend)
# For GCS-backed profiles
DOCFLOW_PROFILES_BACKEND=gcs
DOCFLOW_PROFILES_BUCKET=your-profiles-bucket
DOCFLOW_PROFILES_PREFIX=profiles/

# For filesystem-backed profiles
# DOCFLOW_PROFILES_BACKEND=fs
# DOCFLOW_PROFILES_ROOT_DIR=/absolute/path/to/profiles-root
# DOCFLOW_PROFILES_PREFIX=profiles/

# Catalog cache (seconds)
DOCFLOW_CATALOG_CACHE_TTL=600

# Execution controls for /extract
DOCFLOW_MAX_WORKERS=8
DOCFLOW_DEFAULT_WORKERS=4

# Optional Pub/Sub topic for async result publishing (not required for local runs)
# DOCFLOW_PUBSUB_TOPIC_RESULTS=projects/your-project/topics/docflow-results

# Optional: SDK/CLI helpers (if you run the CLI in remote mode)
# DOCFLOW_MODE=remote
# DOCFLOW_ENDPOINT=http://localhost:8080
# DOCFLOW_PROFILE_DIR=/absolute/path/to/local/profiles
